// Bumped on every shipped change to web/ shell assets or server-visible
// behavior. Surfaced via GET /version (plain text) and embedded in the
// authed page footer so users can spot stale caches.
export const APP_VERSION = 'v15'
