# Rust / Cargo build artifacts
src-tauri/target/
# Tauri 2 codegen (capabilities schema, etc.)
src-tauri/gen/
# Cargo lockfile is intentionally committed for apps, but the agent
# sandbox has no Rust toolchain to generate it — it is produced on the
# first `cargo build` during the manual macOS smoke.
src-tauri/Cargo.lock
